import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.radar_repo.app',
  appName: 'radar-repo',
  webDir: './repo_radar_app/dist'
};

export default config;
